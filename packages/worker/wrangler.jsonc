{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "stickynote-worker",
  "main": "src/index.ts",
  "compatibility_date": "2026-05-01",
  "observability": {
    "enabled": true,
  },
  // Non-secret runtime config. Override per environment as needed.
  // ALLOWED_ORIGINS is a comma-separated list of full origins.
  // CLERK_PUBLISHABLE_KEY is non-secret; set per environment.
  //
  // IMPORTANT: after deploying to your own account, replace ALLOWED_ORIGINS
  // with your real dev origin(s). The localhost defaults are for this repo's
  // own `wrangler dev` only. See packages/worker/DEPLOY.md.
  "vars": {
    "ALLOWED_ORIGINS": "http://localhost:5173,http://localhost:5174",
  },
  // CLERK_SECRET_KEY, CLERK_PUBLISHABLE_KEY come from `.dev.vars` in dev and
  // from `wrangler secret put …` in deployed envs (secret key only).
  // CLERK_ISSUER=dev + DEV_BEARER + DEV_USER_NAME enable the local-dev
  // static-bearer escape hatch. See `.dev.vars.example`.
  //
  // The `database_id` below is a placeholder. The "Deploy to Cloudflare"
  // button provisions a fresh D1 and rewrites this field; manual deploys
  // should run `wrangler d1 create stickynote` and paste the returned ID.
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "stickynote",
      "database_id": "00000000-0000-0000-0000-000000000000",
      "migrations_dir": "migrations",
    },
  ],
}
